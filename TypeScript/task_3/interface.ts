// Define type 'RowID' which equals to a number
export type RowID = number;

// Define an interface for 'RowElement'
export interface RowElement {
    firstName: string;
    lastName: string;
    age?: number;
}